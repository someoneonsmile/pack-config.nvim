-- ----------------------------------------------------------------------
--    - filter_nil -
-- ----------------------------------------------------------------------

M.filter_nil = function(...)
  return M.fn.unpack(vim.tbl_filter(M.predicate.not_nil, { ... }))
end
